# 小鲸鱼喜欢你 0.5.1 项目交接日志

## 本版目标

这是 Android 手机端的完整检查版。重点是保留酒馆原有能力，同时让“小女友”成为独立、可控、不会虚构手机权限的模块。

## 小女友当前真实能力

1. 使用用户在手机内配置的 DeepSeek 或其他 OpenAI 兼容 API 聊天，不依赖电脑中转。
2. 独立人设、亲密度、性格参数、事实记忆、共同回忆、破甲词库和独立聊天目录。
3. 无聊值每三分钟增加一点；达到 60 且用户开启“主动联系”后，可在小女友自己的聊天中写入主动消息并发送通知。
4. 用户可明确要求安排 1～5 条主动消息。
5. 真实联网搜索：手机直接请求 Bing 搜索页，读取最多五条摘要，再把结果隐藏回传给模型组织回答。
6. 打开手机上已安装的应用，但看不到应用内部页面，也不会自动点击。
7. 图片：主模型支持多模态时直接发送；纯文本模型可选 Gemini Vision 先生成中文图片描述。
8. 语音：Android 语音识别输入、系统或 OpenAI 兼容 TTS 朗读、Live2D 口型同步。
9. Live2D：内置六套官方示例模型；支持用户导入含 `.model3.json` 的 ZIP、表情、动作、视线和口型。

## 明确不提供的手机权限

- 不注册无障碍服务，不读取屏幕文字，不自动点击或输入。
- 不读取、隐藏、删除或修改用户相册和文件。
- 不调节音量、亮度，不代发微信、短信或付款。
- 不通过电脑或 OpenClaw 中转小女友任务。

相关旧实现仍可能作为上游酒馆代码保留在源码中，但小女友纯净模式有运行时白名单，设置与清单也不暴露读屏/自动化入口。

## 小女友与酒馆隔离

- 小女友进入时启用 `activateGirlfriendIsolation()`。
- 清空全局系统预设、Persona 描述、世界书、作者注释和扩展注入，只使用小女友角色提示词与动态状态。
- 小女友使用独立聊天存储键 `girlfriend_card.png`。
- 小女友与酒馆分别保存 Live2D 选择：`selected_model_girlfriend` 与 `selected_model`。
- 酒馆原有角色卡、预设、正则、世界书、群聊、图片生成、备份等能力不接入小女友人设。

## 本版修复

- 新版九页中文教程首次安装/升级自动出现一次，并可从设置再次打开。
- 全部 885 个资源字符串存在简体中文翻译。
- 恢复六套内置 Live2D 模型的列表与默认选择。
- Android 15 主动联系服务类型修正，避免 `dataSync` 六小时上限。
- 新图标、启动图、应用内头像和小女友外观描述统一为蓝发小鲸鱼少女。
- 提示词强调正确性、诚实工具状态与技术问题透明说明，减少故意装傻和过度卖萌。

## 安全与发布警告

- API、图片理解、TTS 和联网搜索内容会发送给用户选择的第三方服务商；不要上传敏感资料。
- API Key 仅应由用户在本机配置，源码与发布日志不得包含真实密钥。
- 用户提供的图标原图权属未经独立核验；仅建议个人、非商业测试，权利人提出要求时应及时移除。
- Live2D 示例模型权利归 Live2D Inc.，发布时必须保留 `NOTICE.txt` 与指定版权说明。
- 软件按原样提供，模型回答、第三方服务与小米后台策略都可能变化；升级前应先备份。

## 后续电脑端建议

电脑端单独做“小女友”客户端，不与手机版酒馆合并。可在用户明确授权的沙盒目录内实现文件恶作剧、桌面状态和更强自动化；默认禁用，逐项可撤销，并完整记录操作日志。不要把电脑端高权限能力重新塞回手机 APK。
