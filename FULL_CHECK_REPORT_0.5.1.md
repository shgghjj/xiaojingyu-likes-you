# 小鲸鱼喜欢你 0.5.1 完整检查报告

检查日期：2026-08-04

## 结论

本版已通过源码审计、自动测试、资源完整性检查和 APK 构建检查。由于检查时没有手机或模拟器连接到 ADB，尚未完成真机点击式冒烟测试；这项限制不会被写成“已验证”。

## 已修复问题

1. 首次启动教程使用新的完成标记 `completed_xiaojingyu_v2`，旧版升级后会重新出现一次；设置中的“帮助”直接打开同一个 `TutorialScreen`。
2. 教程改为九页完整中文，内容与手机轻量版真实权限一致。
3. 恢复六套内置 Live2D 官方示例模型及默认选中逻辑，修复“文件存在但列表为空”。
4. 主动联系服务由 Android 15 限时的 `dataSync` 类型改为用户开启的 `specialUse` 类型。
5. 新版蓝发小鲸鱼图标同步到启动图、桌面图标、应用内头像与小女友外观提示词。
6. 提示词强化准确回答、真实工具结果、技术问题透明说明，避免过度卖萌、装傻或提前宣布任务成功。

## 功能核对

| 项目 | 检查结果 |
|---|---|
| 首次教程 | 新安装/升级自动显示一次；可跳过；设置可重新打开 |
| 中文 | 默认简体中文资源 885/885，无缺失键 |
| 小女友与酒馆隔离 | 独立提示词、聊天目录、Live2D 选择；不注入酒馆 Persona、世界书、作者注释或扩展 |
| 手机工具白名单 | 仅联网搜索、打开应用、小女友主动消息 |
| 联网搜索 | 手机直接 HTTPS 请求 Bing；本机实测 HTTP 200，解析到 5 个结果块 |
| 无聊值 | 每 3 分钟增加 1，60 触发阈值；设置页显示进度、说明、清零与主动联系开关 |
| 主动消息 | 写入小女友独立 JSONL 聊天并发通知；用户可要求 1～5 条 |
| 图片 | 多模态模型原生发送；文本模型可用 Gemini Vision 先转中文描述 |
| 语音 | 麦克风按需授权、Android 语音识别输入、TTS 输出与 Live2D 口型电平接口 |
| Live2D | 六个内置模型清单、ZIP 导入路径校验、250MB/1200 文件限制、动作/表情/视线/口型 |
| 角色酒馆 | 角色卡、预设、正则、世界书、多人聊天、图片生成、导入和备份入口保留 |
| 权限边界 | Manifest 不注册无障碍服务；小女友不读屏、不改文件、不调系统设置、不代发外部消息 |
| 密钥 | 源码扫描未发现 DeepSeek/OpenAI、Gemini 或 GitHub 格式的真实密钥 |

## 自动验证

- 单元测试：44 项，通过 44，失败 0，跳过 0。
- Live2D 资源：6 个 `model3.json`，缺失引用文件 0。
- 中文资源：默认键 885，简体中文键 885，缺失 0。
- 搜索解析：Bing 测试请求 HTTP 200，识别结果块 5。
- 完整 Lint 已执行到分析阶段，但因当时系统可用内存不足而未完成报告生成；随后独立执行最终编译、44 项测试和 APK 打包并成功。此项不冒充“Lint 全通过”。
- 最终 APK：版本 0.5.1、versionCode 50100、包名 `com.xiagu.aicompanion`；六个 Live2D 清单均已装入 APK。

## 未能自动证明的部分

- 小米 K80 Pro 的通知、后台节电、系统 TTS/语音识别和 WebView/GPU 表现必须在真机上点击测试。
- DeepSeek、Gemini、语音与图片生成依赖用户自己的 API Key、余额、地区网络与服务商可用性。
- 第三方角色卡、预设、正则和 Live2D ZIP 的兼容性取决于具体文件内容。

## 建议真机验收顺序

1. 覆盖安装后确认版本为 0.5.1，并确认新版教程自动出现。
2. 设置 API，发送普通问题和一个实时新闻问题，确认出现真实联网工具结果。
3. 到小女友设置确认无聊值 UI、主动联系开关、六套 Live2D 模型列表。
4. 打开 Live2D 舞台，依次切换模型并点“测试说话口型”。
5. 测试语音输入、朗读、发送图片和 Gemini Vision 回退。
6. 进入酒馆导入一张角色卡和预设，确认小女友人设与酒馆内容互不串线。

若真机出现崩溃，请立即导出“设置 → 调试日志”，并记录具体入口、点击动作和时间；不要只凭截图猜测原因。
